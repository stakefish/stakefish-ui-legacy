import { Meta, ColorPalette, ColorItem, IconGallery, IconItem } from "@storybook/addon-docs/blocks";

import Icon, { iconList } from "../components/Icon";
import colors from "../theme/colors";

<Meta title="Welcome 👋" />

# 👋 Welcome to stakefish UI

stake.fish lightning design system for React.

## Documentation

- [Contribution guidelines](https://github.com/stakefish/stakefish-ui/blob/master/docs/contribution-guidelines.md)
- [Getting started with `@stakefish/ui`](https://github.com/stakefish/stakefish-ui/blob/master/docs/getting-started.md)

## Colors

## Lv1: Palette

<ColorPalette>
  <ColorItem title="LightGreen" subtitle="theme.palette.lightGreen" colors={colors.lightGreen} />
  <ColorItem title="Green" subtitle="theme.palette.green" colors={colors.green} />
  <ColorItem title="Red" subtitle="theme.palette.red" colors={colors.red} />
  <ColorItem title="Gray" subtitle="theme.palette.gray" colors={colors.gray} />
</ColorPalette>

## Lv2: Tokens

<ColorPalette>
  <ColorItem title="Primary" subtitle="theme.palette.primary" colors={colors.primary} />
  <ColorItem title="PrimaryAlt" subtitle="theme.palette.primaryAlt" colors={colors.primaryAlt} />
  <ColorItem title="Secondary" subtitle="theme.palette.secondary" colors={colors.secondary} />
  <ColorItem title="Error" subtitle="theme.palette.error" colors={colors.error} />
  <ColorItem title="Success" subtitle="theme.palette.success" colors={colors.success} />
  <ColorItem title="Text" subtitle="theme.palette.text" colors={colors.text} />
  <ColorItem title="Icon" subtitle="theme.palette.icon" colors={colors.icon} />
  <ColorItem title="Button" subtitle="theme.palette.button" colors={colors.button} />
  <ColorItem title="Link" subtitle="theme.palette.link" colors={colors.link} />
</ColorPalette>

## Icons

<IconGallery>
  {Object.keys(iconList).map((iconKey) => (
    <IconItem name={iconKey} key={iconKey}>
      <Icon iconKey={iconKey} />
    </IconItem>
  ))}
</IconGallery>
